# Backend: Health Check Background Task
# Wird in ~/trading-app/backend/ als Router /api/missioncontrol/ eingebunden.
# Dieser Ordner enthält nur standby-Tasks, die nicht im Haupt-Backend liegen.

# Platzhalter — der eigentliche Health-Checker läuft im FastAPI-Backend von trading-app.
